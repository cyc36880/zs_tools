# 简介
用于检查比较内存中的数据
可以实现内存数据变化或与要比较的数据相同时，判断为真，进而执行某些操作

# 注意事项
- compare_data 与 data 的内存大小必须一致
- 当 compare_data 不为空时，会使用 compare_data 与 data 比较; 否则会使用内置的hash算法计算是否产生变化。这种算法是可能将两个不同的数据但计算出相同的hash值的，只是概率比较低。
